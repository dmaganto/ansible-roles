---
# tasks file for test
- name: "Add vim package [CentOS]"
  package:
    name: vim
    state: present
  when: ansible_facts['os_family'] == "RedHat"
- name: "Add vim package [Ubuntu]"
  apt:
    name: vim
    update_cache: yes
    state: present
  when: ansible_facts['os_family'] == "Debian"
