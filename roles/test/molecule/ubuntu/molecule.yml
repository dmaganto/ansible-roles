---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: ubuntu
    image: pycontribs/ubuntu
    pre_build_image: true
provisioner:
  name: ansible
lint: |
  set -e
  yamllint .
  ansible-lint
verifier:
  name: ansible
